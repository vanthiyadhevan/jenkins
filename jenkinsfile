pipeline {
    agent any

    stages {
        stage('Hello World') {
            steps {
                echo 'Hello, World!'
                echo 'this is by Vickey'
                echo 'new SG SETUP ONLY HTTPS'
            }
        }
    }

    post {
        always {
            echo 'This will always run after the pipeline finishes.'
        }

        success {
            echo 'Pipeline was successful!'
        }

        failure {
            echo 'Pipeline failed.'
        }
    }
}
